# v0.6.0 本地验证记录

本目录保存重新测得的本地基准与探针结果，关联当前八技能目录及 0.6.0 运行时。这些文件不代表 GitHub Release、Packages 或跨平台 CI 已发布或通过。

| 检查 | 结果 | 数据 |
|---|---|---|
| 指令静态计数 | 7 组场景均完成；短篇保守对照的新版指令量高于上游入口下限 22.45%。仅计算冷加载的指定文件，不等于实际账户用量或作品质量。 | [说明](tokens.md) · [逐文件记录](tokens.json) |
| 规模探针 | 400 章／2,000 卡与 4,000 章／20,000 卡，分别在 strict 与 local 模式完成，共 4 组。使用合成章节与状态数据，每项操作仅一次预热测量。 | [完整记录](scaling.json) |
| 数据迁移探针 | 3 类由 schema 1 运行时生成的中文合成书库完成迁移及回滚检查，原始目录保持不变。未以真实历史书库代替样本。 | [完整记录](migration.json) |

指令计数使用 `tiktoken 0.14.0` 的 `o200k_base`，上游源固定在 `4daac79077928d0d5ba0eda93e46ce68dfcd40ae`。规模与迁移记录含被测运行时文件哈希；发布检查会把这些哈希与当前文件核对，但不会自动重跑探针。数据只说明所列本地场景的结果。

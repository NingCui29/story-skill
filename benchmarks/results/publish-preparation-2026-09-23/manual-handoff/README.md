# 发布准备与人工交接核验（2026-09-23）

本记录对应 v0.5.11 开发工作区；不是 GitHub Release、Packages、本机日常安装或七猫／番茄后台验收。

本轮修复了发布账本结构校验漏检：prepared 专用唯一索引或冻结清单触发器缺失、弱化时，查询和恢复会拒绝继续。修复了材料包回执在核验期间被改写或替换却仍返回可用的竞态。幂等复用清单时刷新本次正式稿核对时间，同时保持冻结清单、准备时间和内容指纹不变。

新增 `publish-compare`：只读比较作者提供的单章标题、正文及可选“作者的话”副本与冻结清单；仅统一换行符，缺少字段明确标为未核对，并重新检查当前正式稿。结果只证明本地文本比较，不证明平台账号、作品、草稿或发布状态。根据 2026-09-23 核对的[番茄小说网用户协议](https://fanqienovel.com/protocal/agreement)和[七猫作家助手用户服务协议](https://zhushou.qimao.com/writer-rules/68464fdfe4a81e7ec312f874/)，未经相应许可不实施第三方自动化登录、上传或回读。

验证结果：

- `python3 -B -m unittest discover -s tests -q`：744 项，727 通过、17 项按环境条件跳过，0 失败、0 错误。未在 Windows 原生环境或真实作家后台执行。
- 发布相关子集：122 项通过、1 项按环境条件跳过；包括账本结构变更、回执修改／替换及副本逐字段比较的回归。
- `skill-creator` 的 `quick_validate.py`：`story-skill-publish` 有效。系统 Python 原先缺少 PyYAML，临时安装到 `/tmp` 后运行；没有更改全局 Python 环境。
- `git diff --check`：通过。
- 最终开发 ZIP：38 个技能载荷文件，SHA-256 `e5da692162ddd057548ae1d6746d6076e0d95636fe8a58926b4b85b3cb8d24fd`。隔离项目安装显示 8 个技能、38 个文件；安装副本中的 `story.py --version` 返回 `0.5.11`，`publish-compare --help` 可调用。另有 59 项打包与安装测试通过。这些均为本地构建及隔离试验，不代表发布或本机安装升级。

未验收：平台账号／作品身份、章节草稿写入与真实远端回读、审核提交、定时上线、许可覆盖范围、Windows 原生运行。后续远端自动化必须先解决平台许可并进行单章真实验收；作者自行从官方后台提供的副本可继续作本地比较，但不能充当独立平台回执。
